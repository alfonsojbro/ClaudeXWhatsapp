# raw/

Captures land here. Filename `<type>-<kebab-topic>.md`, types: `idea`, `bookmark`, `article`, `note`, `braindump`, `person`, `decision`, `email`.
First two lines are always:

```
source: <where it came from>
captured: YYYY-MM-DD
```
