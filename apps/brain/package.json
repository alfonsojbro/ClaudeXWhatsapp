{
  "name": "@cxw/brain",
  "version": "0.0.1",
  "private": true,
  "description": "Brain: Claude Agent SDK loop, router, confirm gate, media pipeline.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "@cxw/scheduler": "workspace:*",
    "@cxw/shared": "workspace:*",
    "chrono-node": "^2.10.0",
    "sharp": "^0.35.4",
    "zod": "^4.5.4"
  }
}
