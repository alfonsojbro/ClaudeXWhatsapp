{
  "name": "@cxw/bridge",
  "version": "0.0.1",
  "private": true,
  "description": "WhatsApp bridge: Baileys link, SQLite store, inbound bus, send API.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@cxw/shared": "workspace:*"
  }
}
