{
  "name": "@cxw/ops",
  "version": "0.0.1",
  "private": true,
  "description": "Operations layer: health checks, alerts, retention purge, cost cap, kill switch.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "cxw-ops": "./bin/cxw-ops.js"
  },
  "scripts": {
    "start": "tsx src/cli.ts sentinel",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "test": "vitest run"
  },
  "dependencies": {
    "nodemailer": "^7.0.0",
    "pino": "^9.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.0",
    "tsx": "^4.23.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.0"
  }
}
